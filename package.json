{
  "name": "employeetracking",
  "version": "1.0.0",
  "description": "PeopleConnect HR - Employee Tracking System",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node index.js",
    "render-build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "dev:backend": "cd backend && node index.js",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "dependencies": {
    "express-validator": "^7.3.1",
    "http": "^0.0.1-security",
    "mongoose": "^9.2.1",
    "multer": "^2.0.2",
    "node-fetch": "^2.7.0",
    "path": "^0.12.7",
    "socket.io-client": "^4.8.3",
    "winston": "^3.19.0"
  },
  "devDependencies": {
    "electron": "^40.4.0",
    "nodemon": "^3.1.11"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
